// Package types will hold cross-cutting value types shared across the
// Secrets Bridge core module (identifiers, label selectors, error kinds).
//
// This package is a placeholder during scaffolding; concrete types will be
// added as the surrounding packages stabilize.
package types
